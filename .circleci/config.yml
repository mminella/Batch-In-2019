# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/repo

    environment:
      # Customize the JVM maximum heap limit
      MAVEN_OPTS: -Xmx3200m

    steps:
      - checkout

#      # Download and cache dependencies
#      - restore_cache:
#          keys:
#            - v3-dependencies-{{ checksum "pom.xml" }}
#            # fallback to using the latest cache if no exact match is found
#            - v3-dependencies-
#
#      - run: mvn dependency:go-offline
#
#      - save_cache:
#          paths:
#            - ~/.m2
#          key: v3-dependencies-{{ checksum "pom.xml" }}
#
#      # run tests!
#      - run: mvn -s .circleci.settings.xml deploy -DAWS_DEFAULT_REGION=us-east-1

      - run:
          command: |
            HASH="$(git rev-parse HEAD)"
            curl "https://mminella-data-flow-server.apps.pcfone.io/apps/task/deloit-job/$HASH" -i -X POST -d "uri=maven%3A%2F%2Fcom.github.mminella.Batch-in-2019%3Abatch-job%3A$HASH"
            curl "https://mminella-data-flow-server.apps.pcfone.io/apps/task/deloit-job/$HASH" -i -X PUT -H 'Accept: application/json'
